# A simple conf test file to prove different
# server_name config scenarios

# Same port, different server_names -> OK
server {
	listen			8080;
	server_name		host1.com;
	root			html/web1;
}

server {
	listen			8080;
	server_name		host2.com;
    autoindex       on;
	root			html/web2;
}


# Same port, same server_names -> FAIL
server {
	listen			8080;
	server_name		host1.com;
	root			html/web1;
}

server {
	listen			8080;
	server_name		host2.com;
    autoindex       on;
	root			html/web2;
}

# Different port, same server_names -> OK
server {
	listen			8080;
	server_name		host1.com;
	root			html/web1;
}

server {
	listen			8080;
	server_name		host2.com;
    autoindex       on;
	root			html/web2;
}

# Different port, different server_names -> OK
server {
	listen			8080;
	server_name		host1.com;
	root			html/web1;
}

server {
	listen			8080;
	server_name		host2.com;
    autoindex       on;
	root			html/web2;
}

# no server_names -> OK
server {
	listen			8080;
	server_name		host1.com;
	root			html/web1;
}

# no server_names, same port -> FAIL
server {
	listen			8080;
    autoindex       on;
	root			html/web2;
}

server {
	listen			8080;
    autoindex       on;
	root			html/web2;
}

# no server_names, different port -> OK
server {
	listen			8080;
    autoindex       on;
	root			html/web2;
}

server {
	listen			8081;
    autoindex       on;
	root			html/web2;
